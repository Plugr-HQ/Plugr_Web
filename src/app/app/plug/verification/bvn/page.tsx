// Verification Hub item: BVN. A static segment, so it takes precedence over the [item] placeholder
// route for this slug.
'use client';

import { BvnScreen } from '@/src/components/plug/BvnScreen';

export default function Page() {
  return <BvnScreen />;
}
