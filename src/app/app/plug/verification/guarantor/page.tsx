// Verification Hub item: Guarantor. A static segment, so it takes precedence over the [item]
// placeholder route for this slug.
'use client';

import { GuarantorScreen } from '@/src/components/plug/GuarantorScreen';

export default function Page() {
  return <GuarantorScreen />;
}
