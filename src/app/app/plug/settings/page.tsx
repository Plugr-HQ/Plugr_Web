// src/app/app/plug/settings/page.tsx — Settings tab (not live at launch)
'use client';

import { SettingsScreen } from '@/src/components/plug/SettingsScreen';

export default function Page() {
  return <SettingsScreen base="/app" />;
}
