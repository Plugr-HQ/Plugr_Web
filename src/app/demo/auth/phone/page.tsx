// src/app/demo/auth/phone/page.tsx — AUTH-02 (Plug side, mirrored into /demo)
'use client';

import { PhoneScreen } from '@/src/components/plug/PhoneScreen';

export default function Page() {
  return <PhoneScreen base="/demo" />;
}
